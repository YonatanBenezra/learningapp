/** MVP practice flow — assessment gate, guest Q1–3, login gate at Q4, sync on auth. */
export const MVP_PRACTICE_MODE = true;

/** Default route after auth when no redirect param. */
export const PRACTICE_PATH = '/problems';
