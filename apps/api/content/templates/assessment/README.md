# Assessment pool (`asmt-*`)

Verified-assessment exercises. **Never** add slugs to `published-slugs.json`.

VA1 pool: `asmt-001` … `asmt-012` (6 RAG + 6 Eval). Generated from practice exercises via:

```bash
node scripts/content/generate-va1-pool.mjs
```

Sitting definition: `content/assessments/va1-q3-2026.json` (4 sampled, 90 min, one per Pro season).
