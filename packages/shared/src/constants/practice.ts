/** Guest free-tier limit before login gate (see docs/MVP requirements). */
export const FREE_PROBLEM_LIMIT = 3;

/** localStorage key for guest practice session bundle. */
export const GUEST_PRACTICE_STORAGE_KEY = 'bina-practice-guest';

/** Legacy guest session id key (skill assessments). */
export const GUEST_SESSION_STORAGE_KEY = 'bina-guest-session-id';
